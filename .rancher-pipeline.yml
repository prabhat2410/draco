stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: nifi-ngsi-resources/docker/Dockerfile
      buildContext: .
      tag: draco-devops
      pushRemote: true
      registry: 192.168.100.178:5000
timeout: 60
notification: {}
